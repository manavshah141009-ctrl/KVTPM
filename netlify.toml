[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# The Netlify Next.js plugin handles serverless functions automatically
[[plugins]]
  package = "@netlify/plugin-nextjs"
